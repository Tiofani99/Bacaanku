<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.id.bacaanku">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bacaanku.NoActionBar">
        <activity
            android:name=".ui.category.NewsCategoryActivity"
            android:exported="true" />
        <activity
            android:name=".ui.detail.NewsDetailActivity"
            android:exported="true"
            android:theme="@style/Theme.Bacaanku.Detail" />
        <activity
            android:name=".ui.splash.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Bacaanku" />
    </application>

</manifest>